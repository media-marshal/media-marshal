---
description: Media Marshal 项目路线图与阶段规划 - 由 PM 维护，用于版本方向和优先级协调
alwaysApply: false
---

# Media Marshal 项目路线图

> 本文件由 PM 维护，记录项目阶段目标、版本方向、优先级和进入条件。
> 本文件不是 ADR，也不替代具体功能设计。进入开发前，涉及架构、数据模型、接口边界或产品交互的事项仍需讨论并写入 `design-decisions.mdc`。
> 开发 Agent 完成落地后，应将实际实现、偏离设计和遗留问题追加到 `implementation-log.mdc`；实际生效接口同步更新 `api-contracts.mdc`。

## 文档状态

- 规划日期：2026-07-13
- 当前版本：`v0.2.10`
- 当前阶段：核心流程已形成闭环，正在从试用版向可稳定发行版本过渡
- 路线图性质：方向与优先级规划；具体版本号可根据试用反馈调整

## 一、当前项目判断

Media Marshal 已经完成媒体自动整理工具的核心闭环，不再处于功能原型阶段。

当前已经具备：

- 文件发现、guessit 解析、TMDB 匹配、置信度判断和自动整理。
- Queue 低置信度人工确认、识别信息编辑、重新匹配、全局搜索和批量确认。
- 已完成任务修正、目标资产重命名和 `CORRECTED -> DONE` 修正历史链路。
- 普通电影、普通剧集、连续多集文件和电影蓝光原盘目录整理。
- 中文 / 双语标题、父目录上下文和多 Query 匹配优化。
- 路径模板变量、可选片段、后端预览、安全渲染和非法路径保护。
- MOVE、COPY、HARD_LINK、SYMBOLIC_LINK 及保存前文件操作能力校验。
- 字幕、NFO、封面、MD5 等常见附属文件处理。
- Sample 目录与同目录 Sample 视频静默剪枝。
- TMDB HTTP 代理运行时配置。
- 监控路径配置导入导出。
- 首次初始化、系统重置、WebSocket 状态推送和中英文界面。

当前最主要的问题已经从“核心功能是否存在”转变为：

- 外部用户能否顺利部署、升级、备份和排错。
- 版本发布和数据库升级是否稳定、可重复。
- 任务发生异常后是否容易理解、恢复和追踪。
- 项目能否从任务流水进一步演进为可靠的媒体资产管理基础。

## 二、路线图原则

后续版本遵循以下优先级原则：

1. 先解决发布、升级、备份和诊断问题，再继续扩大功能范围。
2. 优先增强已有核心流程的稳定性和可恢复性，不以堆叠媒体类型作为主要目标。
3. 媒体资产视图必须建立独立资产语义，不能长期直接把任务流水当成媒体库状态。
4. 播放统计和清理建议必须建立在可靠的媒体资产索引之上。
5. 删除、移动、清理等高风险媒体管理操作，必须晚于权限、审计、二次确认和隔离 / 回滚机制。
6. Webhook、认证和外部集成必须先明确安全边界，不能暴露无保护的文件操作能力。
7. Media Marshal 保持“媒体整理与维护工具”定位，不扩展为下载器、播放器或媒体服务器替代品。

## 三、阶段规划

### 阶段 A：发行硬化

建议版本：`v0.2.11`，最高优先级。

目标：让当前功能真正具备公开发布、外部试用和安全升级条件。本阶段原则上不增加大型业务功能。

计划内容：

- 按当前版本重写并校准 README 中英文内容和版本信息。
- 完成 `docs/deployment.md`，覆盖 Compose 部署、挂载路径、权限、代理、升级、备份和回滚。
- 完成 `docs/faq.md`，覆盖文件发现、TMDB、Queue、路径冲突、文件操作、NAS 和安全部署问题。
- 明确 stable / beta / alpha Compose 文件、Docker 镜像和 tag 的发布规则。
- 补齐 `v0.2.5` 至当前版本的 Git tag / Release 发布策略，避免代码版本与发行标签脱节。
- 执行完整端到端发行验收，并将结果写入 `implementation-log.mdc`。
- 验证普通电影、普通剧集、多集、Queue、修正、蓝光、附属文件、四种文件操作、代理、导入导出和升级场景。
- 明确 SQLite 数据文件的备份、恢复和回滚操作，并实际验证备份可恢复。
- 清理项目文档中的过期状态，例如已经落地但仍被列为“待实现”的 ADR-027、ADR-028。

阶段完成条件：

- 新用户只依赖 README、部署文档和 FAQ 即可完成首次部署。
- 正式镜像可以通过固定版本 tag 安装，不依赖含义漂移的 `latest`。
- 至少完成一次旧版本数据库升级、备份恢复和核心流程回归。
- 当前支持与不支持的能力在 README、官网、FAQ 和页面文案中保持一致。

### 阶段 B：任务管理与运维体验

建议版本：`v0.3.x`，下一阶段主功能方向。

目标：从“Dashboard 查看近期结果”升级为“用户能够定位、理解和处理整个任务生命周期”。

建议能力：

- 新增完整任务管理页，支持后端分页、状态筛选、路径搜索、时间筛选和错误筛选。
- 提供修正历史链路查看能力，清楚展示原任务与修正后任务。
- 设计失败任务重试、重新扫描和重新匹配的边界，避免错误复用 Queue 或源文件修正语义。
- 建立扫描作业模型，展示手动扫描 / 周期扫描的开始时间、进度、发现数量、跳过数量和失败原因。
- 扩展结构化错误码，至少区分 TMDB、Parser、权限、目标目录、跨文件系统硬链接、符号链接、模板和 NFO 错误。
- 在前端提供面向用户的错误解释和处理建议。
- 在初始化页和系统设置页增加 TMDB、代理和 Parser 连通性检测。
- 在任务详情中展示附属文件处理失败等当前只存在于日志中的信息。

阶段完成条件：

- 用户不读取后端日志，也能判断大多数任务为什么失败以及下一步如何处理。
- 长时间扫描和后台任务具有明确状态，不再只依赖短暂提示或 WebSocket 事件。
- 所有重试、修正和重新扫描操作都有清晰且互不混淆的语义。

### 阶段 C：工程基础与升级治理

建议与 `v0.3.x` 并行推进，不作为独立的大型产品版本。

目标：降低后续增加数据模型、任务状态和文件操作能力时的回归与升级风险。

计划内容：

- 建立持续集成，至少执行后端测试、前端类型检查、前端生产构建和 Parser 基础检查。
- 为前端核心流程增加自动化测试，优先覆盖初始化、路径规则、Queue 和任务修正。
- 为 Parser 增加请求 / 响应契约、常见电影和剧集文件名解析测试。
- 引入正式数据库迁移机制，逐步替代只依赖 `hibernate.ddl-auto=update` 和临时表重建迁移。
- 建立数据库升级测试矩阵，至少覆盖最近一个公开版本到当前版本。
- 评估任务修正流程中数据库事务、TMDB 请求和 SQLite 单连接之间的等待问题。
- 增加最小化端到端测试，使用临时目录验证发现、匹配、确认、文件操作和历史记录。
- 根据实际加载性能拆分前端主包；该项低于正确性和升级安全。

阶段完成条件：

- 每次提交或合并都能自动发现后端、前端和 Parser 的基础回归。
- 数据库结构变更具有版本号、迁移顺序和失败处理方式。
- 新版本发布前可重复执行升级测试和核心端到端回归。

### 阶段 D：媒体资产索引与只读媒体库

建议版本：`v0.4.x`，中期战略方向。

目标：从任务处理工具扩展为能够理解“当前媒体库中实际有哪些资产”的整理与维护工具。

第一阶段只做只读能力：

- 建立独立 `MediaAsset` 或等价资产模型，不直接使用 `MediaTask` 代替媒体库状态。
- 记录当前有效目标路径、媒体类型、资产类型、TMDB ID、季集、来源 WatchRule、文件大小、整理时间和存在状态。
- 保留资产与创建任务、修正任务之间的关联关系。
- 提供电影、剧集和蓝光资产的只读列表、筛选、搜索和详情页。
- 提供目标文件存在性核对、孤立任务和未知文件提示。
- 支持重新扫描、重新匹配、重新按模板预览等低风险维护入口。

本阶段暂不支持：

- 直接删除媒体文件。
- 无保护地批量移动或合并资产。
- 自动清理孤儿文件。
- 把 Media Marshal 变成通用 NAS 文件管理器。

进入高风险文件管理前，必须先具备权限控制、操作审计、二次确认、隔离区 / 回收站和硬链接 / 符号链接语义说明。

### 阶段 E：媒体服务器统计与清理建议

建议版本：`v0.5.x` 或更后，建立在阶段 D 的资产索引之上。

目标：将媒体服务器的播放数据映射到 Media Marshal 资产，帮助用户理解资源使用情况并做出整理决策。

建议顺序：

1. Emby。
2. Jellyfin。
3. Plex；必要时通过 Tautulli 获取统计数据。

第一阶段只读展示：

- 播放次数。
- 最近播放时间。
- 播放进度或累计播放时长。
- 从未播放、长期未播放、热门资源和大体积低使用资源。
- 清理候选建议及生成原因。

第一阶段不自动删除、不自动移动、不自动归档。播放数据应允许关闭同步，并明确用户维度数据的最小化存储和隐私边界。

## 四、按反馈决定优先级的方向

以下能力有明确价值，但不应默认插队到阶段 A 之前。是否提前取决于试用反馈数量和影响范围。

### Webhook 与外部集成

- 外部工具通知 Media Marshal 扫描指定文件、目录或 WatchRule。
- API Token、鉴权、限流、路径边界和审计必须与 Webhook 同时设计。
- 适合解决部分 NAS 文件事件不可靠，或与下载工具自动化流程衔接的问题。

### 媒体资产类型扩展

- ISO 镜像整理及其修正能力。
- 剧集蓝光原盘的季集语义和整理规则。
- 裸 `BDMV` 识别。
- 蓝光根目录外旁挂 NFO、poster、folder 等文件处理。
- 蓝光原盘结构完整性校验。

### 匹配与模板增强

- Parser 原始结果快照和识别历史。
- 特殊缩写、地区版本、重制版和例外词典。
- 置信度评分解释与调试信息展示。
- 演员、导演、制片公司、评分、片长和多国家等模板 / NFO 字段。

### 网络与安全增强

- SOCKS、HTTPS 代理、代理认证和海报代理 / 缓存。
- 内置用户认证、API Token、访问控制和多用户权限。
- 在内置认证完成前，继续推荐仅部署于本机、可信局域网或 VPN 环境。

## 五、近期执行顺序

建议按以下顺序推进：

1. 完成阶段 A 的发行文档、版本策略、备份恢复和端到端验收。
2. 收集第一轮试用用户反馈，对问题按“无法部署、数据风险、核心流程失败、体验问题、功能建议”分类。
3. 为阶段 B 的任务管理与运维能力形成独立 ADR，确定任务页、扫描作业、错误码和重试边界。
4. 与阶段 B 并行引入 CI、数据库迁移和关键流程自动化测试。
5. 阶段 B 稳定后，再为独立媒体资产模型形成 ADR。
6. 媒体资产索引稳定后，才进入媒体服务器播放统计和清理建议。

## 六、当前不建议优先投入的事项

- 在发行资料和升级保障未完成前，大规模增加媒体类型。
- 在没有独立资产模型前直接开发播放统计和自动清理。
- 在没有鉴权、审计和隔离机制前提供批量删除或移动媒体文件。
- 仅为消除构建警告而优先进行大规模前端重构。
- 在单实例家庭部署仍满足需求时提前迁移 PostgreSQL；应先解决迁移机制和 SQLite 使用边界。
- 在用户反馈不足时同时建设 Emby、Jellyfin 和 Plex 三套深度集成。

## 七、维护规则

- 路线图只记录阶段方向、优先级和进入条件，不记录具体接口和代码实现。
- 某项进入近期开发前，由 PM 与用户确认范围，并在需要时新增或更新 ADR。
- 已完成事项不在本文件记录实现细节，只更新阶段状态并链接对应 ADR / 实现日志。
- 试用反馈可能调整阶段内部顺序，但不得绕过数据安全、路径安全和文件操作保护原则。
- 每个正式版本发布后，由 PM 复核一次本路线图并更新当前阶段。
