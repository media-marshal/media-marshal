---
description: Java 后端编程规范和模式约定
globs: backend/**/*.java
alwaysApply: false
---

# Backend 编程规范

## 包结构（`com.mediamarshal.*`）
- `config/` — Spring 配置类（@Configuration）
- `controller/` — REST 控制器，薄层，仅校验参数 + 调用 service
- `service/pipeline/` — 核心流程编排（MediaProcessPipeline）
- `service/settings/` — 所有配置读取必须通过 `SettingsService.get()`，禁止直接用 `@Value`
- `service/matcher/` — MetadataMatcher 接口 + 各数据源实现
- `service/rename/` — FileOperationStrategy 接口 + 各策略实现
- `model/entity/` — JPA 实体，用 `@PrePersist`/`@PreUpdate` 维护时间戳
- `model/dto/` — 数据传输对象，API 入参/出参用 DTO，不直接暴露实体

## API 响应
所有接口返回 `ApiResponse<T>`，不裸返回对象：
```java
// ✅
return ApiResponse.ok(data);
return ApiResponse.fail("Not found");

// ❌
return data;
```

## 配置读取
```java
// ✅ 通过 SettingsService（支持三源优先级）
String apiKey = settingsService.get("tmdb.api-key", "");

// ❌ 直接读环境变量或 @Value
@Value("${media-marshal.tmdb.api-key}") String apiKey;
```

## 扩展新文件操作策略
1. 新建类实现 `FileOperationStrategy`，标注 `@Component`
2. 实现 `execute(Path source, Path target)` 和 `getType()`
3. 无需修改任何现有代码，`RenameService` 自动通过 Map 注入

## 扩展新元数据来源
1. 新建类实现 `MetadataMatcher`，标注 `@Component`
2. 实现 `search()`、`getById()`、`getSourceName()`

## 日志规范

使用 `@Slf4j`（Lombok），禁止 `System.out.println`。

### 日志级别约定
```java
log.info("Processing file: {}", file);          // 关键业务节点
log.warn("Low confidence: {}", confidence);     // 非致命异常情况
log.error("Failed to process: {}", file, e);   // 异常必须作为最后参数，完整堆栈
log.debug("TMDB raw response: {}", json);       // 仅 debug 模式下有意义的细节
```

### Debug 模式开关

通过配置项 `media-marshal.debug` 控制 debug 日志的打印，
避免生产环境输出大量无用日志影响性能和可读性。

**配置**（`application.yml`）：
```yaml
media-marshal:
  debug: ${MEDIA_MARSHAL_DEBUG:false}
```

**注入方式**（优先使用 `SettingsService`，与三源优先级保持一致）：
```java
// ✅ 推荐：运行时可通过 Web UI 或环境变量动态切换
boolean isDebug = Boolean.parseBoolean(settingsService.get("debug", "false"));
if (isDebug) {
    log.debug("guessit raw response: filename={}, result={}", filename, result);
}

// ✅ 也可使用 Spring 配置属性注入（服务启动时固定）
@Value("${media-marshal.debug:false}")
private boolean debugMode;
```

**Debug 日志应包含的内容**：
- guessit 原始解析结果（完整 JSON）
- TMDB 请求参数和原始响应
- 置信度计算过程（各字段得分明细）
- 文件操作前的 source/target 完整路径
- WebSocket 推送的事件内容

### 注释规范
- 类级注释：说明职责、关键设计决策、扩展方式
- 方法级注释：说明参数语义、返回值含义、异常场景
- 禁止写"做了什么"的注释（代码本身已表达），只写"为什么这样做"

## WebSocket 推送
所有需要通知前端的事件通过 `EventPublisher` 发布，不直接调用 `SimpMessagingTemplate`：
```java
eventPublisher.publishTaskDone(task);
eventPublisher.publishAwaitingConfirmation(task);
```
