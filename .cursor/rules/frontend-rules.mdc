---
description: Vue 3 前端编程规范和组件约定
globs: frontend/src/**/*.{vue,ts}
alwaysApply: false
---

# Frontend 编程规范

## 技术栈
Vue 3 Composition API + TypeScript + Element Plus + Pinia + vue-i18n

## 组件规范
- 始终使用 `<script setup lang="ts">`（Composition API），禁止 Options API
- 页面组件放 `views/`（命名：`XxxView.vue`）
- 共享组件放 `components/`

## 类型
- 所有类型定义集中在 `src/types/index.ts`
- 禁止使用 `any`，使用 `unknown` 后类型收窄

## API 调用
- 通过 `src/api/*.ts` 调用，禁止在组件中直接使用 axios
- 错误由 `http.ts` 拦截器统一处理（ElMessage.error），组件不需要重复处理网络错误
```typescript
// ✅
const res = await mediaApi.listTasks()
tasks.value = res.data.data

// ❌
const res = await axios.get('/api/tasks')
```

## 状态管理
- 使用 Pinia stores（`src/stores/`）管理全局状态
- 使用 `storeToRefs()` 解构 store 保持响应性
```typescript
// ✅
const { tasks, loading } = storeToRefs(mediaStore)

// ❌ 失去响应性
const { tasks } = mediaStore
```

## 国际化
- 所有用户可见文字必须通过 `t('key')` 国际化，禁止硬编码中文或英文字符串
- key 结构：`模块.子键`，枚举值用大写（与后端保持一致）
```typescript
// ✅
t('task.status.DONE')
t('nav.queue')

// ❌
'已完成'
```

## 版本号与 ReleaseNote
- 应用版本号唯一来源是仓库根目录 `VERSION`，前端不得从 `package.json` 自行读取展示版本。
- 新增用户可见功能、修复用户可感知问题、优化交互/性能/文案/展示逻辑，或接口契约变化影响前端行为时，必须同步更新 ReleaseNote。
- ReleaseNote 结构索引维护在 `frontend/src/meta/releaseNotes.ts`，用户可见文案分别维护在 `frontend/src/i18n/releaseNotes/zh.ts` 与 `frontend/src/i18n/releaseNotes/en.ts`。
- 发布版本变化时，只修改根目录 `VERSION`，然后运行 `node scripts/sync-version.mjs` 同步包元数据，确保前端、后端与 parser 读取同一版本。

## WebSocket
- 组件通过 `onTaskEvent()` 订阅，在 `onUnmounted` 调用返回的取消函数
```typescript
const unsubscribe = onTaskEvent((event) => {
  mediaStore.updateTaskStatus(event.taskId, event.status)
})
onUnmounted(unsubscribe)
```

## 任务完成后的文档同步（必须执行）

每完成一个功能模块，必须做以下两件事，否则视为任务未完成：

1. **追加实现记录到 `implementation-log.mdc`**，按文件内的模板格式填写
2. **开发前必须先读 `api-contracts.mdc`**，以此为准对齐后端接口，不以后端代码注释为准

> 发现接口与 `api-contracts.mdc` 不一致时，在 `implementation-log.mdc` 的"遗留问题"区说明，由 PM 协调后端修正。

---

## Element Plus 使用
- 优先使用 `el-message`、`el-message-box` 做反馈，不自定义弹窗
- 表格数据量大时使用虚拟滚动（`el-table-v2`）
